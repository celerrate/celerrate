//! Rule registration is composition-root vocabulary: no plugin can
//! reach the registry through the facade; plugin-registered rules
//! are deferred.
use celerrate_plugin::RuleRegistry;

fn main() {}
